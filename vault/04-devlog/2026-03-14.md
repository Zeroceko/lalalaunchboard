# Dev Log - 2026-03-14

## Completed

- cleaned repo layout and moved specs under `specs/`
- removed old kiro-based duplicate spec copies
- set the product name to `Lalalaunchboard`
- scaffolded the Next.js app foundation
- added Docker dev/test workflow
- implemented auth UI shell
- added auth API routes and hCaptcha integration points
- verified local and Docker build flows
- added Supabase CLI project config and core SQL migrations
- implemented auth-to-profile sync trigger and RLS policies
- verified local database reset, lint, and auth/RLS smoke flow
- implemented protected app shell, dashboard, new app form, and apps CRUD routes
- added plan-limit enforcement and graceful schema-blocker states for hosted env mismatch
- added fallback-backed Contentful layer, CMS API routes, and revalidation hook
- implemented first checklist screen, progress helpers, and checklist toggle API
- implemented item detail slide-over, deliverable CRUD routes, and storage bucket migration
- implemented post-launch routine routes, weekly log UI, and post-launch page
- implemented Markdown/PDF export utilities, export route, and export page
- implemented middleware protection, root-level toast notifications, and workspace navigation cleanup
- added `vitest` + `fast-check` property-test foundation with first export/progress tests
- refreshed the first visible frontend pass across landing, auth, dashboard, new app, and workspace screens
- introduced a reusable workspace hero pattern and upgraded routine/export/checklist surfaces
- upgraded checklist interaction surfaces: item cards, detail panel, deliverable form, and the signed-in app shell header
- added property tests for deliverable validation/merge, routine merge, and export content integrity
- added a shared `WorkspaceNotice` surface for schema/profile/not-found blockers and re-verified the stable `lint -> build -> typecheck` flow
- restored committed ESLint config so `npm run lint` works again in this workspace snapshot
- added `.env.local.example` back into the repo and synced the current status docs around missing hosted env
- synced `README.md`, `HANDOFF.md`, `specs/roadmap.md`, `specs/tasks.md`, and `specs/flows.md` to the actual `Development/` root and current MVP state
- made `Flow 0` explicit in `specs/flows.md` so landing -> auth ownership is no longer implied-only

## Current Position

App CRUD, CMS fallback, checklist, deliverables, post-launch routine, export, middleware, settings helper, portfolio management panel, and the first serious frontend polish pass are now in the repo. The next major milestone is completing the deeper landing/checklist polish, then moving into hosted Supabase push and the remaining optional property-test coverage.

## Blockers

No coding blocker locally.

Hosted Supabase push still requires:

- `SUPABASE_ACCESS_TOKEN` or an authenticated Supabase CLI session
